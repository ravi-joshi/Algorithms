/*
Write a program to reverse all substrings in a string
*/

// algorithm
// extract individual substring using strtok()
// define two pointers, one pointing at the start of
// the substr and the second at the end of the substr
// swap the characters
// get the next substr using strtok() - repeat
// until reaching the end of the string

#include <stdio.h>
#include <string.h>

void revall(char s[])
{
	char *ss;
	
}

void main(void)
{
	char s[] = "Ravi Joshi";
	revall(s);
} 

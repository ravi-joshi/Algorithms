/*
remove duplicates from an array
*/

#include <stdio.h>

void dup(int a[], int sz)
{
	// if the array is unsorted, sort the array
	// and then the elements can be moved
	
}

void main(void)
{
	int a[] = {1, 2, 3, 4, 4, 5, 5, 5, 6};
	int sz = sizeof(a)/sizeof(a[0]);
	dup(a, sz);
}


